# Feature Requirements

Feature requirements are organised by processing domain to minimise the context required for a task. Use [the requirements index](README.md) to select the applicable files. Requirement IDs and their wording are preserved in those files.

Superseded requirements are removed from the active requirements set; use Git history when historical context is necessary.
